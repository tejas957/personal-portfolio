diff --git a/src/pages/blog/index.astro b/src/pages/blog/index.astro
index 26e9bef..eed0891 100644
--- a/src/pages/blog/index.astro
+++ b/src/pages/blog/index.astro
@@ -3,16 +3,18 @@ import Base from '../../layouts/Base.astro';
 import { posts } from '../../data/posts.js';
 
 const sorted = [...posts].sort((a, b) => new Date(b.date) - new Date(a.date));
+const rawBase = import.meta.env.BASE_URL;
+const base = rawBase.endsWith('/') ? rawBase : `${rawBase}/`; // ensures '/personal-portfolio/' not '/personal-portfolio'
 ---
 <Base title="Blog">
   <div class="min-h-screen">
     <header class="border-b border-line">
       <nav class="mx-auto flex max-w-4xl items-center justify-between px-6 py-5 font-mono text-sm">
-        <a href="/" class="text-paper hover:text-amber transition-colors">~/portfolio</a>
+        <a href={base} class="text-paper hover:text-amber transition-colors">~/portfolio</a>
         <div class="flex gap-6 text-muted">
-          <a href="/#projects" class="hover:text-amber transition-colors">projects</a>
-          <a href="/blog" class="text-amber">blog</a>
-          <a href="/#contact" class="hover:text-amber transition-colors">contact</a>
+          <a href={`${base}#projects`} class="hover:text-amber transition-colors">projects</a>
+          <a href={`${base}blog`} class="text-amber">blog</a>
+          <a href={`${base}#contact`} class="hover:text-amber transition-colors">contact</a>
         </div>
       </nav>
     </header>
@@ -34,7 +36,7 @@ const sorted = [...posts].sort((a, b) => new Date(b.date) - new Date(a.date));
             ></span>
             <p class="font-mono text-xs text-muted">{p.date}</p>
             <h2 class="mt-2 font-display text-xl font-semibold">
-              <a href={`/blog/${p.slug}`} class="hover:text-amber transition-colors">{p.title}</a>
+              <a href={`${base}blog/${p.slug}`} class="hover:text-amber transition-colors">{p.title}</a>
             </h2>
             <p class="mt-2 max-w-xl text-sm leading-relaxed text-paper/80">{p.summary}</p>
             <div class="mt-3 flex flex-wrap gap-2">
@@ -42,7 +44,7 @@ const sorted = [...posts].sort((a, b) => new Date(b.date) - new Date(a.date));
                 <span class="rounded-full border border-line px-2.5 py-0.5 font-mono text-xs text-muted">{t}</span>
               ))}
             </div>
-            <a href={`/blog/${p.slug}`} class="mt-4 inline-block font-mono text-xs text-amber hover:underline">read →</a>
+            <a href={`${base}blog/${p.slug}`} class="mt-4 inline-block font-mono text-xs text-amber hover:underline">read →</a>
           </li>
         ))}
       </ol>
diff --git a/src/pages/index.astro b/src/pages/index.astro
index 6aff3e9..7702c81 100644
--- a/src/pages/index.astro
+++ b/src/pages/index.astro
@@ -1,6 +1,9 @@
 ---
 import Base from '../layouts/Base.astro';
 
+const rawBase = import.meta.env.BASE_URL;
+const base = rawBase.endsWith('/') ? rawBase : `${rawBase}/`; // ensures '/personal-portfolio/' not '/personal-portfolio'
+
 // ---- EDIT ME: your data lives here ----
 const name = 'Tejas Senthil';
 const role = 'CS Student';
@@ -13,7 +16,7 @@ const links = [
 //Resume TO-DO
   { label: 'GitHub', href: 'https://github.com/tejas957' },
   { label: 'LinkedIn', href: 'https://www.linkedin.com/in/tejas-senthil-04a82a286/' },
-  { label: 'Resume', href: '/resume.pdf' },
+  { label: 'Resume', href: `${base}resume.pdf` },
   { label: 'Email', href: 'mailto:tejassenthilcollege@gmail.com' },
 ];
 
@@ -73,7 +76,7 @@ const skills = {
         <a href="#top" class="text-paper hover:text-amber transition-colors">~/portfolio</a>
         <div class="flex gap-6 text-muted">
           <a href="#projects" class="hover:text-amber transition-colors">projects</a>
-          <a href="/blog" class="hover:text-amber transition-colors">blog</a>
+          <a href={`${base}blog`} class="hover:text-amber transition-colors">blog</a>
           <a href="#skills" class="hover:text-amber transition-colors">skills</a>
           <a href="#contact" class="hover:text-amber transition-colors">contact</a>
         </div>
@@ -141,7 +144,7 @@ const skills = {
                 <a href={p.demo} class="text-amber hover:underline">demo →</a>
                 <a href={p.repo} class="text-muted hover:text-paper hover:underline">source →</a>
                 {p.blogSlug && (
-                  <a href={`/blog/${p.blogSlug}`} class="text-teal hover:underline">writeup →</a>
+                  <a href={`${base}blog/${p.blogSlug}`} class="text-teal hover:underline">writeup →</a>
                 )}
               </div>
             </li>
